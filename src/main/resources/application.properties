spring.application.name=task-board-backend

# PostgreSQL DB Connection using environment variables (Render/Supabase)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- OPTIONAL: Supabase Direct Values (for local testing only) ---
# spring.datasource.url=jdbc:postgresql://db.iimumnnfnbliikuxunbv.supabase.co:5432/postgres
# spring.datasource.username=postgres
# spring.datasource.password=Aryan@12345

# --- OPTIONAL: SQL Server Configuration (if needed) ---
# spring.datasource.url=jdbc:sqlserver://LAPTOP-BC7UB6S9:1433;databaseName=taskDB;encrypt=true;trustServerCertificate=true
# spring.datasource.username=sa
# spring.datasource.password=Aryan@12345
# spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
